apply plugin: 'java'
apply plugin: 'maven'

group = 'com.github.chck'
version = '1.0'

description = """"""

sourceCompatibility = 1.7
targetCompatibility = 1.7


repositories {
    maven { url "http://repo.spring.io/milestone/" }
    maven { url "http://repo.maven.apache.org/maven2" }
}
dependencies {
    compile group: 'org.springframework.boot', name: 'spring-boot-starter-integration', version: '1.1.0.RC1'
    compile group: 'org.jsoup', name: 'jsoup', version: '1.7.3'
}
jar {
    manifest {
        attributes 'Main-Class' : 'hello.CrawlerApp'
    }
}
